# Outputs are regenerated by run_all.R; only the small curated copies in results/
# are tracked (see README).
MethaneData_outputs/
figures/
*.Rproj.user
.Rhistory
.RData
.Rapp.history

# Input data is far too large for git; see README "Data availability".
Aircraft/
*.ict
*.nc
*.tif
*.tar.gz
*.zip

# OS / editor cruft
.DS_Store
._*
*~
.vscode/
